{
  "name": "cloudbuds",
  "version": "1.0.0",
  "scripts": {
    "start": "cd client && yarn run build && cd .. && node server --exec nodemon",
    "start-local": "npm run build-client && PORT=8080 node server"
  },
  "dependencies": {
    "express": "^4.16.2",
    "express-pino-logger": "^4.0.0",
    "twilio": "^3.41.1"
  }
}
